const base_url = 'https://localhost:44315/api/';
const orders_endpoint = base_url + 'orders/';
const skus_endpoint = base_url + 'skus/';

export default {
    orders_endpoint : orders_endpoint,
    skus_endpoint : skus_endpoint
}